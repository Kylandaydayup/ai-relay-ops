apiVersion: v1
kind: Service
metadata:
  name: {{ include "newapi-compat-gateway.fullname" . }}
  labels:
    {{- include "newapi-compat-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      {{- if and (eq .Values.service.type "NodePort") .Values.service.nodePort }}
      nodePort: {{ .Values.service.nodePort }}
      {{- end }}
  selector:
    app.kubernetes.io/name: {{ include "newapi-compat-gateway.name" . }}
    app.kubernetes.io/instance: {{ include "newapi-compat-gateway.instance" . }}
