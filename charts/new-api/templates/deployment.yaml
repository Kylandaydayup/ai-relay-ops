apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "new-api.fullname" . }}
  labels:
    {{- include "new-api.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "new-api.name" . }}
      app.kubernetes.io/instance: {{ include "new-api.instance" . }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "new-api.name" . }}
        app.kubernetes.io/instance: {{ include "new-api.instance" . }}
      annotations:
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- if .Values.postgresWait.enabled }}
      initContainers:
        - name: wait-for-postgres
          image: "{{ .Values.postgresWait.image.repository }}:{{ .Values.postgresWait.image.tag }}"
          imagePullPolicy: {{ .Values.postgresWait.image.pullPolicy }}
          command:
            - sh
            - -c
          args:
            - >-
              until pg_isready -h {{ .Values.postgresWait.host }} -p {{ .Values.postgresWait.port }};
              do echo waiting for postgres; sleep {{ .Values.postgresWait.intervalSeconds }}; done
      {{- end }}
      containers:
        - name: new-api
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.containerPort }}
              protocol: TCP
          env:
            {{- range $key, $value := .Values.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            - name: SQL_DSN
              valueFrom:
                secretKeyRef:
                  name: {{ include "new-api.secretName" . }}
                  key: SQL_DSN
            - name: REDIS_CONN_STRING
              valueFrom:
                secretKeyRef:
                  name: {{ include "new-api.secretName" . }}
                  key: REDIS_CONN_STRING
            - name: SESSION_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "new-api.secretName" . }}
                  key: SESSION_SECRET
            - name: CRYPTO_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "new-api.secretName" . }}
                  key: CRYPTO_SECRET
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readiness.path }}
              port: http
            initialDelaySeconds: {{ .Values.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds }}
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.liveness.path }}
              port: http
            initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
          {{- if .Values.persistence.enabled }}
          volumeMounts:
            - name: data
              mountPath: /data
          {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- if .Values.persistence.enabled }}
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: {{ include "new-api.fullname" . }}-data
      {{- end }}
